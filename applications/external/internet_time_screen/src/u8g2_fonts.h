#pragma once

#include <stdint.h>

/* Large font for @beats (includes '@' + digits). */
extern const uint8_t u8g2_font_logisoso28_tr[2646];

/* Local time line (digits, colon, AM/PM letters). */
extern const uint8_t u8g2_font_logisoso16_tr[1564];
